# Netlify SPA fallback: serve index.html for client-side routes so deep links
# (/about, /features, /installation) resolve with 200 instead of 404. Real
# files (sitemap.xml, robots.txt, og.png, assets) are served before this rule.
/*  /index.html  200
